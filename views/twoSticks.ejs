<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Details</title>
</head>
<body>
    <h1> Restaurant Details</h1>
    <p><strong>Name:</strong> <%= twoSticks.name %> </p>
    <p><strong>Menu:</strong></p>
    <ul>
        <% if (twoSticks.menu) { %>
            <% for (let i = 0; i < twoSticks.menu.length; i++) { %>
                <li><%= twoSticks.menu[i] %></li>
                <% } %>
        <% } else { %>
            <li> No menu items availabele</li>
            <% } %>
    </ul>
    <p> Questions? Comments? Contact us down below!</p>
    <p><strong>Phone:</strong> <%= twoSticks.phone %> </p>
    <p><strong>Email:</strong> <%= twoSticks.email %> </p>
    <a href="/"> Back to home </a>
</body>
<%- include('./partials/footer.ejs') %>
</html>